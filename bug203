Find this section in your Dockerfile

Around line 30-40:

ENV PIP_DISABLE_PIP_VERSION_CHECK=1

RUN --mount=type=cache,target=/root/.cache/pip/http \
    python3 -m pip install -U pip==${PIP_VERSION}
🔧 Replace with this
ENV PIP_DISABLE_PIP_VERSION_CHECK=1

RUN apt-get update && \
    apt-get install -y python3-pip && \
    python3 -m pip install -U pip==${PIP_VERSION}
⚡ Better Production Fix (Recommended)

A cleaner way used in production:

RUN apt-get update && \
    apt-get install -y python3-pip && \
    python3 -m ensurepip && \
    python3 -m pip install --upgrade pip
